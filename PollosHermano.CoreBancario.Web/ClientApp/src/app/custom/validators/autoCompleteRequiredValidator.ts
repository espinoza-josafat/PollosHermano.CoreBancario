////import { ValidationErrors, ValidatorFn, AbstractControl } from "@angular/forms";
//////import { SigmaAutoCompleteComponent } from "../components/sigma-autocomplete/sigma-autocomplete.component";
//////import Utils from "../helpers/Utils";

////export function autoCompleteRequiredValidator(sigmaAutoCompleteComponent: SigmaAutoCompleteComponent): ValidatorFn {
////    return (control: AbstractControl): ValidationErrors | null => {
////        //debugger
////        if (sigmaAutoCompleteComponent.model) {
////            return null;
////        }
    
////        //const length = control.value.length;
        
////        return { requiredAutocomplete: { component: sigmaAutoCompleteComponent } };
////    };
////}
